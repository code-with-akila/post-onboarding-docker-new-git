apiVersion: v1
kind: Service
metadata:
  name: fresher-mng-service
spec:
  type: LoadBalancer
  selector:
    app: fresher-mng-app
  ports:
  - protocol: TCP
    port: 80          # external port
    targetPort: 5000  # container port

